package middleware

import (
	"github.com/Kaua-Matheus/fitstore/backend/model"
)

// Checar como iremos fazer para adquirir se o usuário existe ou não
func userExists(user model.User) {
	//model.GetUserByLogin()
}

func CreateUser(user model.User) (error) {


	return nil
}